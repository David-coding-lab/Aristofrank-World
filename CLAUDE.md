@AGENTS.md

# CLAUDE.md

@AGENTS.md

## Claude-Specific Notes

- Treat every rule in AGENTS.md above as binding for this project.
- When scaffolding a new route under `app/`, stop and ask whether it's public or
  protected before writing files — do not infer this from the route name alone.
- Before marking a task complete, run lint and type-check (see Commands in
  AGENTS.md) and fix any failures you introduced.
- If a requested change conflicts with the folder structure or priorities above
  (security > speed > SEO), flag the conflict instead of silently picking one.
